Blog post written to `src/content/blog/hyperliquid-vs-solana-defi-momentum.md`. It uses all the real BotIndex data (Solana's 20.0 avg velocity, Base's 21.2, the top movers with actual scores and volumes, zero SURGE/RISING signals), frames the Hyperliquid comparison around architectural differences (CLOB vs AMMs), and maintains the "velocity leads price" thesis throughout. Four FAQ pairs, internal links in every section, ~1,100 words.